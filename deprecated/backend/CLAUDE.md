# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This document is the `backend` claude.md file.

## Naming

- NestJS-style `<name>.<type>.ts` where a type exists (`*.service.ts`, `*.repository.ts`,
  `*.module.ts`), plain kebab-case otherwise.
