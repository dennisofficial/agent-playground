export enum ETaskStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  DROPPED = 'dropped',
}

export enum ESubagentStatus {
  RUNNING = 'running',
  DONE = 'done',
  FAILED = 'failed',
}
