node_modules
dist
*.log
.DS_Store

# Zero's local memory store (SQLite checkpoints/facts) — machine-local, re-created on first run
.data/

# env — never commit secrets
.env
.env.personal
.env.keys
.env*.local
