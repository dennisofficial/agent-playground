{
  "name": "agent-playground-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "AI Slack-employee workforce — pnpm monorepo (NestJS backend + shared + terminal playground).",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "setup": "git submodule update --init --recursive && pnpm install && pnpm build:packages",
    "build:packages": "pnpm --filter \"./packages/**\" run build",
    "daemon:build": "bash backend/scripts/build-daemon.sh",
    "daemon:reset": "bash backend/scripts/reset-sandboxes.sh",
    "docker": "docker compose up",
    "test:unit": "pnpm --filter backend test:unit"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.71.0",
    "prettier": "^3.8.3",
    "tsup": "^8.5.1"
  }
}
