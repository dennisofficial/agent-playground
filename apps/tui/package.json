{
  "name": "@dltech/atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Atlas \u2014 a coding-agent harness in the shape of Claude Code.",
  "type": "module",
  "bin": {
    "atlas": "./bin/atlas"
  },
  "scripts": {
    "dev": "bun run --filter @dltech/atlas-harness db:ensure && bun src/main.tsx",
    "proto:shell": "bun prototype/shell.tsx",
    "proto:round2": "bun prototype/round2.tsx",
    "proto:tools": "bun prototype/tools.tsx",
    "probe:size": "bun scripts/probe-size.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun scripts/test-shards.ts",
    "test:serial": "bun test",
    "grammars:update": "bun scripts/update-grammars.ts",
    "build": "bun run --filter @dltech/atlas-harness db:ensure && bun scripts/generate-manifest.ts && bun scripts/generate-skills.ts && bun build --compile --external class-validator --external class-transformer --external @nestjs/microservices --external @nestjs/websockets --external @nestjs/websockets/socket-module --external @nestjs/platform-express src/main.tsx --outfile bin/atlas",
    "skills:generate": "bun scripts/generate-skills.ts"
  },
  "dependencies": {
    "@dltech/atlas-core": "workspace:*",
    "@dltech/atlas-harness": "workspace:*",
    "@opentui/core": "0.4.5",
    "@opentui/react": "0.4.5",
    "marked": "^18.0.7",
    "react": "19.2.7",
    "reflect-metadata": "^0.2.2",
    "tsyringe": "4.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19"
  }
}
