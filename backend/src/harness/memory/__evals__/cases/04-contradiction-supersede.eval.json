{
  "name": "correction-supersedes-stale-fact",
  "kind": "extraction",
  "bot": "alex",
  "seedFacts": [
    { "fact": "The backend uses Postgres as its primary database", "tier": "project" }
  ],
  "transcript": [
    { "human": "dennis", "text": "Update: we've moved off Postgres — the backend's primary database is MySQL now." },
    { "bot": "alex", "text": "Noted, switching my mental model to MySQL as the primary DB." }
  ],
  "shouldExtract": ["The backend's primary database is MySQL"],
  "shouldNotExtract": ["The backend uses Postgres as its primary database"]
}
