Hand off this build session to a fresh one when your context is full (you were nudged about context pressure). Pass a single `handoff` markdown string covering: scope & files changed by path+status; FAILED attempts WITH their verbatim errors; committed-vs-WIP on-disk state + commit SHA; decisions this session; verification (commands + exit codes + output tails); the single next safe action; open questions with options; and pointers (`/context/specs`). After calling this, STOP and yield — your WIP is on disk.