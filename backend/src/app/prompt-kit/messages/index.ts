/**
 * prompt-kit / messages — Pillar 2: user-message / task-body templates (typed-function templates returning
 * AgentMessage). Populated by Thread 2 (relocated task builders). Empty until then.
 */
export {};
