/**
 * Re-export shim — the tag vocabulary moved to the hub (`prompt-kit/harness/tag-vocabulary.ts`, Thread 3).
 * Kept here so the many existing importers don't churn.
 */
export * from '../prompt-kit/harness/tag-vocabulary';
