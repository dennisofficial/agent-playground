{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declaration": false,
    "outDir": "../../dist"
  },
  "//": "Atlas v2 is a clean room: it sees _core (env/logger/tracing) + _lib (datasource helpers) + its own tree + the entrypoint, but NOT ../harness (v1 orchestration). The harness dirs are deliberately absent from `include` so a stray v1 import fails to compile loudly.",
  "include": ["**/*",
    "./main.ts", "../_core/**/*", "../_lib/**/*", "../shared/**/*"],
  "exclude": [
    "../../node_modules",
    "dist",
    "**/*spec.ts",
    "**/*.test.ts",
    "**/*.eval.ts",
    "**/*.ai.data.ts"
  ]
}
