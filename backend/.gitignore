# compiled output
/dist
/node_modules
*.tsbuildinfo

# logs
*.log

# dotenvx — encrypted .env.*.enc files ARE committed; keys / personal / plaintext-local are NOT
.env
.env.local
.env.personal
.env.keys
!.env.*.enc

# OS / IDE
.DS_Store
