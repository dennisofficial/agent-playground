# compiled output
/dist
/node_modules
*.tsbuildinfo
/coverage

# generated engine bundle (the API rebundles it on boot into dist; the CLI can regenerate it in src)
engine-entrypoint.mjs

# logs
*.log

# dotenvx — encrypted .env.*.enc files ARE committed; keys / personal / plaintext-local are NOT
.env
.env.local
.env.personal
.env.keys
!.env.*.enc

# OS / IDE
.DS_Store
